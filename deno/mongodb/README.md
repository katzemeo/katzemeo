# katzemeo - MongoDB PoC (based on https://github.com/erfanium/deno-deploy-mongo)

## Run from terminal (client)
Run the client to access the server
```
- deno run --allow-net --allow-env --allow-read --watch querySymbol.ts
```